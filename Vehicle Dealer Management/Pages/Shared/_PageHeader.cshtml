@{
    var userRole = ViewData["UserRole"]?.ToString() ?? "";
    var showBackToManager = userRole == "DEALER_MANAGER";
}

@if (showBackToManager)
{
    <div class="page-breadcrumb">
        <a href="/DealerManager/Dashboard" class="back-button">
            <i class="bi bi-arrow-left"></i>
            <span>Quay lại Dashboard Manager</span>
        </a>
    </div>
}

