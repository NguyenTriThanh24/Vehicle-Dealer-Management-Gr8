@page
@model Vehicle_Dealer_Management.Pages.Admin.Reports.ConsumptionModel
@{
    ViewData["Title"] = "Phân tích tốc độ tiêu thụ";
    ViewData["UserRole"] = "EVM_ADMIN";
    ViewData["UserName"] = HttpContext.Session.GetString("UserName") ?? "Admin";
}

<div class="content-header">
    <h1 class="content-title">Phân tích tốc độ tiêu thụ</h1>
    <p class="content-subtitle">Đánh giá mức độ phổ biến và tốc độ bán của từng mẫu xe</p>
</div>

<!-- Summary -->
<div class="grid grid-cols-4 mb-4">
    <div class="stat-card">
        <div class="stat-label">Tốc độ TB</div>
        <div class="stat-value">@Model.AvgConsumptionRate.ToString("F1")</div>
        <div class="text-muted" style="font-size: 0.875rem;">xe/tuần</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Xe bán nhanh</div>
        <div class="stat-value" style="color: var(--success);">@Model.FastMovingCount</div>
        <div class="text-muted" style="font-size: 0.875rem;">mẫu xe</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Xe bán chậm</div>
        <div class="stat-value" style="color: var(--error);">@Model.SlowMovingCount</div>
        <div class="text-muted" style="font-size: 0.875rem;">mẫu xe</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Thời gian TB hết kho</div>
        <div class="stat-value">@Model.AvgDaysToEmpty</div>
        <div class="text-muted" style="font-size: 0.875rem;">ngày</div>
    </div>
</div>

<!-- Chart -->
<div class="card mb-4">
    <div class="card-header">
        <h3 class="card-title mb-0">Tốc độ tiêu thụ theo xe</h3>
    </div>
    <div class="card-body">
        <div class="chart-container">
            <div class="chart-placeholder">
                <i class="bi bi-speedometer2" style="font-size: 4rem; opacity: 0.3;"></i>
                <p>Biểu đồ line chart (Chart.js placeholder)</p>
            </div>
        </div>
    </div>
</div>

<!-- Consumption Table -->
<div class="card">
    <div class="card-header">
        <div class="flex justify-between items-center">
            <h3 class="card-title mb-0">Chi tiết tốc độ tiêu thụ</h3>
            <select class="form-control" style="width: 200px;">
                <option>Tất cả</option>
                <option>Bán nhanh</option>
                <option>Bán chậm</option>
            </select>
        </div>
    </div>
    <div class="card-body">
        <div class="table-container">
            <table class="table">
                <thead>
                    <tr>
                        <th>Mẫu xe</th>
                        <th>Đã bán (30 ngày)</th>
                        <th>Tồn kho hiện tại</th>
                        <th>Tốc độ (xe/tuần)</th>
                        <th>Dự kiến hết kho</th>
                        <th>Phân loại</th>
                        <th>Khuyến nghị</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ConsumptionData)
                    {
                        <tr>
                            <td><strong>@item.VehicleName</strong></td>
                            <td>
                                <strong style="font-size: 1.125rem; color: var(--accent-cyan);">@item.SoldLast30Days</strong>
                                <span class="text-muted">xe</span>
                            </td>
                            <td>@item.CurrentStock xe</td>
                            <td>
                                <strong style="font-size: 1.125rem;">@item.WeeklyRate.ToString("F1")</strong>
                                <span class="text-muted">xe/tuần</span>
                            </td>
                            <td>
                                @if (item.DaysToEmpty.HasValue)
                                {
                                    <span>@item.DaysToEmpty ngày</span>
                                }
                                else
                                {
                                    <span class="text-muted">N/A</span>
                                }
                            </td>
                            <td>
                                @if (item.Speed == "FAST")
                                {
                                    <span class="badge badge-success">⚡ Bán nhanh</span>
                                }
                                else if (item.Speed == "MEDIUM")
                                {
                                    <span class="badge badge-warning">⏱️ Trung bình</span>
                                }
                                else
                                {
                                    <span class="badge badge-error">🐌 Bán chậm</span>
                                }
                            </td>
                            <td>
                                @if (item.Speed == "FAST" && item.CurrentStock < 20)
                                {
                                    <span style="color: var(--error);">
                                        <i class="bi bi-exclamation-circle"></i> Cần nhập thêm
                                    </span>
                                }
                                else if (item.Speed == "SLOW" && item.CurrentStock > 50)
                                {
                                    <span style="color: var(--warning);">
                                        <i class="bi bi-exclamation-triangle"></i> Giảm nhập kho
                                    </span>
                                }
                                else
                                {
                                    <span style="color: var(--success);">
                                        <i class="bi bi-check-circle"></i> Ổn định
                                    </span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

